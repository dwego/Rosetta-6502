MEMORY {
    ROM: start = $E000, size = $2000, file = %O;
}

SEGMENTS {
    CODE:    load = ROM, type = ro;                 # Firmware body starts at $E000
    VECTORS: load = ROM, type = ro, start = $FFFA;  # NMI/RESET/IRQ vectors
}
